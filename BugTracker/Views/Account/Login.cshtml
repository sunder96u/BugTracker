@using BugTracker.Models
@model LoginViewModel
@{
    ViewBag.Title = "Log in";
}
@{ Layout = null;}
@Styles.Render("~/Content/css")
<link href="~/Content/LoginStyle.css" rel="stylesheet" />
<link rel="shortcut icon" href="~/Images/ms-icon-310x310.png" type="image/x-icon">
<link rel="icon" href="~/Images/ms-icon-310x310.png" type="image/x-icon">


<div class="row">
    <div class="col-md-4 col-md-offset-4 well login-page">
        <h2>Log In:</h2>
        <section id="loginForm">
            @using (Html.BeginForm("Login", "Account", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
            {
                @Html.AntiForgeryToken()
                <h4>Use a local account to log in.</h4>
                <hr />
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                <div class="form-group">
                    @Html.LabelFor(m => m.Email, new { @class = "col-md-4 control-label" })
                    <div class="col-md-8">
                        @Html.TextBoxFor(m => m.Email, new { @class = "form-control" })
                        @Html.ValidationMessageFor(m => m.Email, "", new { @class = "text-danger" })
                    </div>
                </div>
                <div class="form-group">
                    @Html.LabelFor(m => m.Password, new { @class = "col-md-4 control-label" })
                    <div class="col-md-8">
                        @Html.PasswordFor(m => m.Password, new { @class = "form-control" })
                        @Html.ValidationMessageFor(m => m.Password, "", new { @class = "text-danger" })
                    </div>
                </div>
                <div class="row">
                    <div class="form-group">
                        <div class="col-md-offset-5 col-md-10">
                            <div class="checkbox">
                                @Html.CheckBoxFor(m => m.RememberMe)
                                @Html.LabelFor(m => m.RememberMe)
                            </div>
                        </div>
                    </div>
                    <div class="center_btn">
                        <div class="col-md-10">
                            <input type="submit" value="Log in" class="btn btn-default" />
                        </div>
                    </div>
                </div>
                <br />
                <div class="center">
                    @Html.ActionLink("Register as a new user", "Register") |
                    @Html.ActionLink("Forgot your password?", "ForgotPassword")

                    @*//Enable this once you have account confirmation enabled for password reset functionality*@
                </div>
            }
        </section>
    </div>
    <div class="col-md-4 col-md-offset-4 well login-page">
        <h2>Use the Demo Users to test what the functionality of each role is:</h2>
        <section id="loginForm2">
            <br />
            <div class="row">
                <div class="col-md-4 col-md-offset-2">
                @using (Html.BeginForm("DemoLogin", "Account", FormMethod.Post))
                {
                    @Html.AntiForgeryToken()
                    @Html.Hidden("Email", "Admin@Mailinator.com")
                    @Html.Hidden("Password", "Abc123!")
                    <button type="submit" class="btn btn-default">Demo Administrator</button>
                }
                </div>
                <div class="col-md-4">
                @using (Html.BeginForm("DemoLogin", "Account", FormMethod.Post))
                {
                    @Html.AntiForgeryToken()
                    @Html.Hidden("Email", "ProjectManager@Mailinator.com")
                    @Html.Hidden("Password", "Abc123!")
                    <button type="submit" class="btn btn-default">Demo Project Manager</button>
                }
                </div>
            </div>
            <div class="row">
                <div class="col-md-4">
                    @using (Html.BeginForm("DemoLogin", "Account", FormMethod.Post))
                    {
                        @Html.AntiForgeryToken()
                        @Html.Hidden("Email", "Developer1@Mailinator.com")
                        @Html.Hidden("Password", "Abc123!")
                        <button type="submit" class="btn btn-default">Demo Developer 1</button>
                    }
                </div>
                <div class="col-md-4">
                    @using (Html.BeginForm("DemoLogin", "Account", FormMethod.Post))
                    {
                        @Html.AntiForgeryToken()
                        @Html.Hidden("Email", "Developer2@Mailinator.com")
                        @Html.Hidden("Password", "Abc123!")
                        <button type="submit" class="btn btn-default">Demo Developer 2</button>
                    }
                </div>
                <div class="col-md-4">
                    @using (Html.BeginForm("DemoLogin", "Account", FormMethod.Post))
                    {
                        @Html.AntiForgeryToken()
                        @Html.Hidden("Email", "Developer3@Mailinator.com")
                        @Html.Hidden("Password", "Abc123!")
                        <button type="submit" class="btn btn-default">Demo Developer 3</button>
                    }
                </div>
             </div>
            <div class="row">
                <div class="col-md-4 col-md-offset-2">
                    @using (Html.BeginForm("DemoLogin", "Account", FormMethod.Post))
                    {
                        @Html.AntiForgeryToken()
                        @Html.Hidden("Email", "Submitter1@Mailinator.com")
                        @Html.Hidden("Password", "Abc123!")
                        <button type="submit" class="btn btn-default">Demo Submitter 1</button>
                    }
                </div>
                <div class="col-md-4">
                    @using (Html.BeginForm("DemoLogin", "Account", FormMethod.Post))
                    {
                        @Html.AntiForgeryToken()
                        @Html.Hidden("Email", "Submitter2@Mailinator.com")
                        @Html.Hidden("Password", "Abc123!")
                        <button type="submit" class="btn btn-default">Demo Submitter 2</button>
                    }
                </div>
            </div>
        </section>
    </div>
    @*<div class="col-md-4">
            <section id="socialLoginForm">
                @Html.Partial("_ExternalLoginsListPartial", new ExternalLoginListViewModel { ReturnUrl = ViewBag.ReturnUrl })
            </section>
        </div>*@
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}